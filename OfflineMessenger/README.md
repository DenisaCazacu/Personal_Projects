A client-server application built in Rust, using async operations, TLS for secure connections, and sqlx for storage. The server handles user authentication, message sending and receiving, and displays notifications upon reconnection.

